# Libraries
```{r}
library(tidyverse)
library(CodeClanData)
library(janitor)

```

# Assign and clean data
```{r}
students <- students_big %>% 
  # Remove variables assumed to be unrelated to the brief
  select(c(region, ageyears, gender, importance_reducing_pollution:importance_internet_access))
```

# Clusters of students for Importance questions

```{r}
kmeans()
```

